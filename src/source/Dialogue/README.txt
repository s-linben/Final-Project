The dialogue for each instance is stored in folders, so you have the constant 

	"dialogue_" + <instance ID>

This will open the folder and initalize the dialogue in the following order, from a sorted version of a-f (dear god pray that the sorting algorithm works).